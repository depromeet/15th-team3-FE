import { css } from '@emotion/react';
import { size } from '@sambad/sds/theme';

export const rePickTextCss = css({ display: 'flex', alignItems: 'center', marginTop: size['5xs'], cursor: 'pointer' });
