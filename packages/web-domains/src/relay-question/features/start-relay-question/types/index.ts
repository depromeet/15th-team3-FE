export interface MyInfoResponse {
  name: string;
  email: string;
  profileImageUrl: string;
  createdAt: string;
  updatedAt: string;
}

export interface MyMeetingsResponse {
  meetingIds: number[];
}
