import { css } from '@emotion/react';

export const buttonWrapperCss = css({
  position: 'fixed',
  bottom: '40px',
  width: '100%',
  left: '50%',
  transform: 'translate(-50%, 0px)',
  padding: '0 20px',
  maxWidth: '600px',
});
